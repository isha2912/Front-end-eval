import React from 'react';
import './Home.scss';

const Home = (() => (
  <div className="wait">
    <div> :((</div>
    <div> Seems a bit empty here</div>
    <bitton className="temp" type="button">Sync </bitton>
  </div>
));
export default Home;
